[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "recur"
version = "0.1.0"
dependencies = ["questionary", "rich"]

[project.scripts]
recur = "Recur.main:main"